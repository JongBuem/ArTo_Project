{
  "name": "test_1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "Scanner": "cd Scanner && npm start",
    "serve": "cd Server && npx nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"npm run Scanner\" \"npm run serve\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.2.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
